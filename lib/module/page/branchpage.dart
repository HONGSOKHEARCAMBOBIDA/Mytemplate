import 'package:flutter/material.dart';

class Branchpage extends StatelessWidget {
  const Branchpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("branch")),);
  }
}