import 'package:flutter/material.dart';

class Currencypage extends StatelessWidget {
  const Currencypage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("currency")),
    );
  }
}